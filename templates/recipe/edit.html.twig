{% extends 'base.html.twig' %}

{% block title %}
	edit {{recipe.slug}}-{{recipe.id}}
{% endblock %}

{% block body %}
	<div class="container row col-6">
		<h1>{{recipe.title}}</h1>
		{{ form(form) }}
	</div>
	
{% endblock %}